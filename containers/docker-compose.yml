version: '3'

services:
    PostgreSQL:
        image: postgres
        container_name: PostgreSQL
        environment:
            - POSTGRES_PASSWORD = prout
        volumes:
            - postgres-data:/var/lib/postgresql/data
        ports:
            - "5432:5432"

volumes:
    postgres-data:

networks:
    ft_transcendence:
        driver: bridge